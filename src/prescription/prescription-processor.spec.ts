import { PrescriptionProcessor } from './prescription-processor';

describe('PrescriptionProcessor', () => {
  it('should be defined', () => {
    expect(new PrescriptionProcessor()).toBeDefined();
  });
});
